{
    "name": "react-seed",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "start": "lerna run start --scope=@react-seed/client",
        "release": "lerna version --conventional-commits"
    },
    "devDependencies": {
        "lerna": "^3.18.3"
    }
}
